spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5433/demo_auth
    username: admin
    password: admin@321
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port:
    8081


application:
  security:
    jwt:
      secret: 88bd01110be524ab76598b512c6a3ffab92cf9bf164cb5fe391183972df3b145
      issuer: auth-service
      expiration-minutes: 30
